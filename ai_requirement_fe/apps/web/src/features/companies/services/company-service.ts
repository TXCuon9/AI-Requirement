import { type CompanySummary } from "@types";
import { type CompanyFilters } from "../types";

export async function searchCompanies(_filters: CompanyFilters): Promise<CompanySummary[]> {
  return [];
}
